//function declaration

function soma (x,y){ //pode ser chamada em qualquer momento
    return x + y
}

//function expression
const sub = function(){//só pode ser chamada depois da declaração 
    return x - y
}

//named function expression

const mult = function mult(x,y){//só pode ser chamada depois da declaração
    return x * y 
}
